import { join } from 'node:path';

export const CONTENT_DIR = join(process.cwd(), 'content');
export const AUDIO_DIR = join(process.cwd(), 'audio');
export const DEFAULT_BUNDLE_OUT = join(process.cwd(), 'bundle');
